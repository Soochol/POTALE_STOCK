default_seed:
  description: "기본 Seed 조건"

  # ╔═══════════════════════════════════════════════════════════════════════╗
  # ║                            Block1 조건                                ║
  # ╚═══════════════════════════════════════════════════════════════════════╝
  block1:
    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                          진입 조건 (Entry)                          │
    # └─────────────────────────────────────────────────────────────────────┘
    entry_surge_rate:           10.0      # 등락률: 14%
    entry_ma_period:            60       # MA기간: 120일 (null이면 MA 조건 전체 skip, 값이 있으면 고가≥MA 체크)
    entry_max_deviation_ratio:  500.0     # 이격도: 500%
    entry_min_trading_value:    300.0     # 거래대금: 300억
    entry_volume_high_days:   null        # 신고거래량: 비활성화 (null = 조건 완전 스킵)
    entry_volume_spike_ratio:   120.0     # 거래량비: 300% (3배)
    entry_price_high_days:    null        # 신고가: 비활성화 (null = 조건 완전 스킵)

    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                          종료 조건 (Exit)                           │
    # └─────────────────────────────────────────────────────────────────────┘
    exit_condition_type:        ma_break  # 종료방식: ma_break | three_line_reversal | body_middle
    exit_ma_period:             60       # 종료MA: 120일

    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                         시스템 설정 (System)                        │
    # └─────────────────────────────────────────────────────────────────────┘
    min_start_interval_days:              20        # 쿨다운: 20일

  # ╔═══════════════════════════════════════════════════════════════════════╗
  # ║                            Block2 조건                                ║
  # ╚═══════════════════════════════════════════════════════════════════════╝
  block2:
    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                     Block2 추가 조건 (Block1 기준)                  │
    # └─────────────────────────────────────────────────────────────────────┘
    volume_ratio:               100.0      # 거래량비: Block1 최고 대비 100%
    low_price_margin:           5.0      # 저가마진: Block1 최고가 대비 5%
    min_candles_after_block1:   2         # 최소캔들: Block1 시작 후 4개
    max_candles_after_block1:   150       # 최대캔들: Block1 시작 후 250개 (약 1년)

    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                Block2 전용 진입 조건 (없으면 Block1 값 사용)         │
    # └─────────────────────────────────────────────────────────────────────┘
    entry_surge_rate:           10.0      # 등락률: 14%
    entry_ma_period:            60       # MA기간: 120일 (null이면 MA 조건 전체 skip)
    entry_max_deviation_ratio:  500.0     # 이격도: 500%
    entry_min_trading_value:    1000.0    # 거래대금: 1000억
    entry_volume_high_days:   null        # 신고거래량: 비활성화 (null = 조건 완전 스킵)
    entry_volume_spike_ratio:   120.0     # 거래량비: 300%
    entry_price_high_days:    null        # 신고가: 비활성화 (null = 조건 완전 스킵)

    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                       Block2 전용 종료 조건                         │
    # └─────────────────────────────────────────────────────────────────────┘
    exit_condition_type:        ma_break  # 종료방식
    exit_ma_period:             60        # 종료MA: 60일
    min_start_interval_days:              20        # 쿨다운: 20일

  # ╔═══════════════════════════════════════════════════════════════════════╗
  # ║                            Block3 조건                                ║
  # ╚═══════════════════════════════════════════════════════════════════════╝
  block3:
    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                     Block3 추가 조건 (Block2 기준)                  │
    # └─────────────────────────────────────────────────────────────────────┘
    volume_ratio:               10.0      # 거래량비: Block2 최고 대비 18%
    low_price_margin:           5.0      # 저가마진: Block2 최고가 대비 5%
    min_candles_after_block2:   10         # 최소캔들: Block2 시작 후 4개
    max_candles_after_block2:   150       # 최대캔들: Block2 시작 후 120개 (약 6개월)

    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                Block3 전용 진입 조건 (없으면 Block1 값 사용)         │
    # └─────────────────────────────────────────────────────────────────────┘
    entry_surge_rate:           10.0      # 등락률: 10%
    entry_ma_period:            60       # MA기간: 120일 (null이면 MA 조건 전체 skip)
    entry_max_deviation_ratio:  500.0     # 이격도: 500%
    entry_min_trading_value:    1000.0    # 거래대금: 1000억
    entry_volume_high_days:   null        # 신고거래량: 비활성화 (null = 조건 완전 스킵)
    entry_volume_spike_ratio:   120.0     # 거래량비: 300%
    entry_price_high_days:    null        # 신고가: 비활성화 (null = 조건 완전 스킵)

    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                       Block3 전용 종료 조건                         │
    # └─────────────────────────────────────────────────────────────────────┘
    exit_condition_type:        ma_break  # 종료방식
    exit_ma_period:             60        # 종료MA: 60일
    min_start_interval_days:              20        # 쿨다운: 20일

  # ╔═══════════════════════════════════════════════════════════════════════╗
  # ║                     Block4 조건 (완화된 조건)                         ║
  # ╚═══════════════════════════════════════════════════════════════════════╝
  block4:
    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                     Block4 추가 조건 (Block3 기준)                  │
    # └─────────────────────────────────────────────────────────────────────┘
    volume_ratio:               10.0      # 거래량비: Block3 최고 대비 10%
    low_price_margin:           5.0      # 저가마진: Block3 최고가 대비 5%
    min_candles_after_block3:   2         # 최소캔들: Block3 시작 후 4개
    max_candles_after_block3:   1000        # 최대캔들: Block3 시작 후 60개 (약 3개월)

    # ┌─────────────────────────────────────────────────────────────────────┐
    # │             Block4 전용 진입 조건 (완화) - Block1 값보다 완화        │
    # └─────────────────────────────────────────────────────────────────────┘
    entry_surge_rate:           10.0       # 등락률: 10% (완화)
    entry_ma_period:            60        # MA기간: 60일 (짧음, null이면 MA 조건 전체 skip)
    entry_max_deviation_ratio:  500.0     # 이격도: 500%
    entry_min_trading_value:    300.0     # 거래대금: 150억 (낮음)
    entry_volume_high_days:   null        # 신고거래량: 비활성화 (null = 조건 완전 스킵)
    entry_volume_spike_ratio:   120.0     # 거래량비: 300%
    entry_price_high_days:    null        # 신고가: 비활성화 (null = 조건 완전 스킵)

    # ┌─────────────────────────────────────────────────────────────────────┐
    # │                       Block4 전용 종료 조건                         │
    # └─────────────────────────────────────────────────────────────────────┘
    exit_condition_type:        ma_break  # 종료방식
    exit_ma_period:             60        # 종료MA: 60일
    min_start_interval_days:              20        # 쿨다운: 20일
